# Backbone-Calendar

## Initial setup

```sh
rbenv install
rbenv rehash
gem update --system
gem list -i ^bundler$ >/dev/null || gem install bundler
```

## Getting started

```sh
bundle install
foreman start
```
